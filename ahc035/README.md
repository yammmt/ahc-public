# AHC035

## `tester` の使い方

`rm` は必要に応じて省略します.

```console
cd tools
rm -f out0000.txt && cargo run -r --bin tester ../target/release/a < in/0000.txt > out0000.txt
```

## `pahcer` の使い方

```console
pahcer run
```
