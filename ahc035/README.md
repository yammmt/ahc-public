# AHC035

## `tester` の使い方

`rm` は必要に応じて省略します.

```console
cd tools
rm -f out0000.txt && cargo run -r --bin tester ../target/release/a < in/0000.txt > out0000.txt
```

## `pahcer` の使い方

```console
pahcer run
```

## 実装

Codex (gpt-5.6-terra medium) に依頼しました.
というかアイデア時点でも Codex に引っ張られました. AI の二年間の進化はすごいです.
